我現在需要製作一個flask的api，請幫助我完成flask的api架構

以下是我需要的api

1. 接收網頁下的點動指令(X,Y,Z)

每次只能接收一個軸的點動。

2. 接收網頁下達的歸零指令

3. 接收網頁下達的最大值指令

4. 接收網頁下達的模式切換指令(有1~4)4種模式

5. 網頁每秒讀取api，此api會開啟serial 去讀取當前的三軸座標、當前旋轉軸模式等，

6. 要輸出一個及時webcam的影像畫面api，由另一個函數來處理geterate_frame()

7. 接收即停指令，

8. 所有的接收指令都要寫入到一個Argument.json檔案中，並由另一個線程來每秒讀取這些指令狀態，並將這些狀態讀出來放在一個dict中